apiVersion: v1
kind: Service
metadata:
  name: {{ include "pwa-apps.fullname" . }}
  labels:
    {{- include "pwa-apps.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pwa-apps.selectorLabels" . | nindent 4 }}

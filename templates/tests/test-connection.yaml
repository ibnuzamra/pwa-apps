apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pwa-apps.fullname" . }}-test-connection"
  labels:
    {{- include "pwa-apps.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pwa-apps.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
